import { AnyExtendedMapper } from "../extended-mapper";
import { Optional } from "../optional";
/**
    Attempts to extract the `Optional` of a `Mapper<>`.
    If not found, returns `unknown`.
*/
export declare type ExtractOptionalOrUnknown<F extends AnyExtendedMapper> = (F extends Optional ? Optional : unknown);
//# sourceMappingURL=extract-optional-or-unknown.d.ts.map