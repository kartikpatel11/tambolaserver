import { AnyExtendedMapper } from "../extended-mapper";
import { Name } from "../name";
/**
    Attempts to extract the `Name<>` of a `Mapper<>`.
    If not found, returns `unknown`.
*/
export declare type ExtractNameOrUnknown<F extends AnyExtendedMapper> = (F extends Name<infer NameT> ? (string extends NameT ? unknown : Name<NameT>) : unknown);
//# sourceMappingURL=extract-name-or-unknown.d.ts.map