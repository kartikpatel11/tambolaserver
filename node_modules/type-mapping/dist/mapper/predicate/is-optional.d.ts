import { MappableInputOf, ExpectedInputOf } from "../query";
import { AnySafeMapper } from "../safe-mapper";
import { Optional } from "../optional";
export declare type IsOptional<F extends AnySafeMapper> = (F extends Optional ? (undefined extends MappableInputOf<F> ? true : false) : false);
export declare type IsExpectedInputOptional<F extends AnySafeMapper> = (IsOptional<F> extends true ? (undefined extends ExpectedInputOf<F> ? true : false) : false);
export declare function isOptional(f: unknown): f is Optional;
//# sourceMappingURL=is-optional.d.ts.map