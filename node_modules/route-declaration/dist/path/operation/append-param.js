"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var path_1 = require("../path");
/**
    regex, if provided, ignores modifiers like `g` and `i`
*/
function appendParam(path, name, regex) {
    if (name.indexOf(":") >= 0) {
        throw new Error("\":\" not allowed in param, " + name);
    }
    if (name.indexOf("/") >= 0) {
        throw new Error("\"/\" not allowed in param, " + name);
    }
    var newRouterPath = path.routerPath + "/:" + name;
    if (regex != undefined) {
        newRouterPath += "(" + regex.source + ")";
    }
    var newParamPart = {
        name: name,
        regex: regex,
    };
    return new path_1.Path(path.parts.concat([newParamPart]), newRouterPath);
}
exports.appendParam = appendParam;
//# sourceMappingURL=append-param.js.map