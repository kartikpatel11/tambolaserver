import { SafeMapper, ExpectedInput, MappableInput } from "../../mapper";
import { FluentMapper } from "../../fluent-mapper";
/**
    TS `number` type is actually a `double` type
*/
export declare function double(): (FluentMapper<SafeMapper<number> & ExpectedInput<number> & MappableInput<string | number | bigint>>);
/**
    Alias for DOUBLE for now.
    JS doesn't have `float` type.

    This means that it is possible
    to send a value to MySQL and
    lose precision.
*/
export declare function float(): FluentMapper<import("../../mapper").Mapper<unknown, number> & ExpectedInput<number> & MappableInput<string | number | bigint>>;
//# sourceMappingURL=index.d.ts.map