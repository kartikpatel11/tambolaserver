import { SafeMapper } from "../../mapper";
export declare type InstanceOfMapper<T> = (SafeMapper<T>);
/**
    If you pass in a bigint object created by
    a polyfill, it will never pass any `instanceof` checks,
    even though the polyfill could be done with an object.
*/
export declare function instanceOf<T>(ctor: new (...args: any[]) => T): (InstanceOfMapper<T>);
//# sourceMappingURL=instance-of.d.ts.map